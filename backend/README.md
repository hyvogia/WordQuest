# Node.js + TypeScript MVC Starter
## Environment Setup
* Create a `.env` file in the project root that contains environment variables, for example:`PORT=5003`
## Run in the local computer
1. Install dependencies: npm install
2. Run: npm run dev
## Build for production
1. Build: npm run build

